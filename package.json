{
  "name": "YAW",
  "version": "1.0.0",
  "description": "Yoast-like Analysis for Webflow",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter common dev\" \"pnpm --filter client dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "common": "pnpm --filter common",
    "client": "pnpm --filter client",
    "server": "pnpm --filter server"
  },
  "keywords": [],
  "author": "Paul Mulligan",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "hono": "^4.7.1",
    "@hono/zod-validator": "^0.4.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
